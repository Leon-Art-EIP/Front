export interface IFilters {
  searchTerm: string;
  artType: string;
  priceRange: string;
  isForSale: boolean;
  sort: string;
  artPage: number;
  artLimit: number;
  artistPage: number;
  artistLimit: number;
}